import requests
import json
import random
import time
from datetime import datetime

# Assuming the server is running, but let's just write the script to hit the endpoints
# Actually, I will run the Flask app locally first
